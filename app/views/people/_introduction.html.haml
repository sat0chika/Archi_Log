.IntroTemple
  .UserInfo
    .UserInfo__icon
      - if @person.icon?
        = image_tag @icon.url, class: 'UserInfo__icon'
    .UserInfo__name
      = @name
    .UserInfo__Text
      = @introduction
      .UserInfo__contents
  .Posts
    -# = link_to "/people/#{current_person.id}"
    .PostsIndex
    = link_to "/people/#{current_person.id}", class: "post-index" do
      投稿一覧
    = link_to new_topic_path do
      新規投稿
    = link_to fab_people_path, class: "fab-index" do
      お気に入り